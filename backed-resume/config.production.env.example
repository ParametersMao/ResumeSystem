NODE_ENV=production
PORT=3000

# Comma-separated allowed origins for browser access.
FRONTEND_URL=https://www.your-domain.com,https://admin.your-domain.com

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=resume_app
DB_PASSWORD=replace-me
DB_DATABASE=resume_system

# JWT
JWT_ACCESS_SECRET=replace-with-a-long-random-string
JWT_REFRESH_SECRET=replace-with-another-long-random-string

# Independent 32-byte key for AES-256-GCM encryption of API/SMTP secrets stored in MySQL.
# Generate once with: openssl rand -base64 32
# Back this key up separately; changing or losing it makes encrypted settings unreadable.
SYSTEM_CONFIG_MASTER_KEY=replace-with-a-base64-encoded-32-byte-key

# Optional first-run bootstrap only. Remove both values after the first successful login.
BOOTSTRAP_ADMIN_USERNAME=
BOOTSTRAP_ADMIN_PASSWORD=

# AI provider
OPENAI_API_URL=https://api.openai.com/v1
OPENAI_API_KEY=replace-me
OPENAI_MODEL=gpt-4.1-mini

# Cloudflare R2 object storage
# Endpoint should be account-level, without the bucket path.
R2_ENDPOINT=https://2cef367ed0d9606a47029b9aa51fa19c.r2.cloudflarestorage.com
R2_REGION=auto
R2_ACCESS_KEY_ID=replace-me
R2_SECRET_ACCESS_KEY=replace-me
R2_BUCKET=cv-assets
R2_PUBLIC_BASE_URL=https://pub-3849403bf41741939821877a7df073c5.r2.dev

# Only used when R2/OSS is not configured and files fall back to local /uploads.
PUBLIC_FILE_BASE_URL=https://api.your-domain.com

# Puppeteer browser path on Linux server
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome
